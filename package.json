{
  "name": "ultrastar",
  "devDependencies": {
    "@ultrastar/tsconfig": "workspace:*",
    "dotenv-cli": "^7.4.2",
    "elysia": "^1.0.22",
    "prettier": "^3.2.5"
  },
  "packageManager": "bun@1.0.17",
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo build",
    "dev": "dotenv -- turbo dev",
    "watch": "dotenv -- turbo run watch",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@elysiajs/eden": "^1.0.14"
  }
}
